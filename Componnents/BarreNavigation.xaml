<UserControl x:Class="CaissePoly.Componnents.BarreNavigation"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="60" d:DesignWidth="800">
    <Grid Background="#F8FAFC" Height="60">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="50"/>
            <ColumnDefinition Width="50"/>
        </Grid.ColumnDefinitions>

        <!-- Date et Heure -->
        <StackPanel Grid.Column="0" Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="DDate" FontWeight="Bold" FontSize="12" Foreground="#334155" HorizontalAlignment="Center"/>
            <TextBlock Text="{Binding CurrentDate}" FontSize="11" Foreground="#EF4444" HorizontalAlignment="Center"/>
            <TextBlock Text="{Binding CurrentTime}" FontSize="11" Foreground="#EF4444" HorizontalAlignment="Center"/>
        </StackPanel>

        <!-- Caissier -->
        <StackPanel Grid.Column="1" Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="Caissier" FontWeight="Bold" FontSize="12" Foreground="#334155"/>
            <TextBlock Text="Admin" FontSize="11" Foreground="#EF4444"/>
        </StackPanel>

        <!-- Caisse -->
        <StackPanel Grid.Column="2" Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="Caisse" FontWeight="Bold" FontSize="12" Foreground="#334155"/>
            <TextBlock Text="C1" FontSize="11" Foreground="#EF4444"/>
        </StackPanel>

        <!-- Nom App -->
        <StackPanel Grid.Column="3" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="" FontWeight="Bold" FontSize="16" Foreground="#F59E0B" Margin="2,0" Width="100"/>
            <TextBlock Text="POLYSOFT" FontWeight="Bold" FontSize="16" Foreground="#3B82F6"/>
            <TextBlock Text="CO" FontWeight="Bold" FontSize="16" Foreground="#F59E0B"/>
        </StackPanel>

        <!-- Spacer colonne vide -->
        <StackPanel Grid.Column="5"/>

        <!-- Home Button -->
        <Button Grid.Column="6" Width="30" Height="30" Background="#E2E8F0" BorderBrush="#CBD5E1" BorderThickness="1" ToolTip="Accueil">
            <Path Data="M5,10 L10,5 L15,10 L13,10 L13,15 L7,15 L7,10 Z" Fill="#334155"/>
        </Button>

        <!-- Close Button -->
        <Button Grid.Column="7" Width="30" Height="30" Background="#EF4444" BorderBrush="Transparent" ToolTip="Fermer">
            <TextBlock Text="✕" FontSize="14" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Button>
    </Grid>
</UserControl>
